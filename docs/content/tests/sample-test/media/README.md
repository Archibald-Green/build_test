# Sample media

The sample test references these relative media paths:

- `linear-functions.svg`
- `meeting-place.mp3`

The image is included as a working relative-media example. The audio file is
intentionally absent so the Stage 2 non-blocking media error state can be tested.
